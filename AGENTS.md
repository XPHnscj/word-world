# 项目维护提示

## 阅读页不可破坏的排版约束

这是一个长期本地使用的学习工具。修改前端时，必须保留以下阅读页结构：

1. 学习日、文章标题、主题元信息沿页面中轴居中。
2. 标题必须允许自然换行，禁止用固定宽度、负位移或 `white-space: nowrap` 强行摆放。
3. “调整意见”输入框与重新生成按钮整体居中，宽度不得超过正文容器。
4. 正文保持独立的居中阅读宽度，不因标题或工具栏改变而左右移动。
5. “显示划线词 / 原文填词 / 检查 / 记录学习日”固定在右下角，禁止改成底部通栏或页面居中工具栏。
6. 桌面端和窄屏端都要检查；窄屏标题不得溢出或贴边。
7. 用户指定只调整标题与意见框时，不得顺带修改文章大框、正文宽度或其他层级；标题和意见框应共享同一中心点。

## 本次错误记录

- 错误：对 `.reading-header` 和 `.reading-fab` 连续追加多组相互冲突的覆盖规则。
- 表现：标题在部分窗口宽度下产生视觉偏移，右下角工具栏被旧的 `left: 50%` 规则拉到页面中间。
- 原因：只检查单条新规则，没有核对完整 CSS 级联顺序和响应式覆盖。
- 具体根因：阅读头部从 Flex 改为 Grid 后，遗留的 `justify-content: space-between` 仍对网格轨道生效，把标题和意见框所在的隐式列推到左侧。改为单列 `minmax(0, 1fr)` 并显式 `justify-content: stretch`。
- 二次错误：在用户只想协调标题与意见框时扩大了修改范围，尝试调整文章大框；收到限定后已撤回。
- 存储错误：重新生成使用 `packs.where("planDay")` 替换当天短文，但旧数据库结构没有为 `planDay` 建索引，导致 AI 已生成后在保存阶段抛出 `SchemaError`，界面误报“生成失败”。数据库 v5 已补索引，日短文替换必须继续使用单一事务，禁止先删后写。
- 修复原则：阅读页最终定位规则集中在 `globals.css` 末尾；修改前先搜索所有同名选择器，优先合并或删除冲突规则，不继续无条件叠加。

## 修改后的最低验证要求

- 运行 `npm test -- --run`。
- 运行 `npm run build`，不得留下 lint 或 TypeScript 警告。
- 至少检查约 1440px 桌面宽度和 800px 以下窄屏宽度。
- 确认标题与意见框中心一致，悬浮操作栏位于右下角，正文没有横向位移。

## 本地数据与启动地址

- IndexedDB 按浏览器 origin 隔离，端口或主机名变化都会表现成一套新的空数据。
- 启动器必须始终使用 `http://127.0.0.1:3000`，禁止在 3001、3002 等端口之间自动切换，也不要改成 `localhost`。
- 已有服务时复用固定地址；固定端口被其他程序占用时应明确报错，不得静默换端口。
- 学习数据、词书、短文和会话记录必须继续写入 IndexedDB；设置和少量计划状态可写入 localStorage。

## 抽词与短文生成约束

- 内置词书抽词必须从未学词中随机选择，并优先分散首字母；禁止再次退化为按词库顺序整批抽取 a、b 等相邻开头词。
- AI 短文参考长度为 75–95 个英文词、4–6 句，允许为自然表达略有浮动；只有明显超过 110 词时才自动要求模型重写一次。
- 短文必须锚定一个具体可想象的场景，至少包含地点、人物动作以及视觉或听觉细节，再自然推进到 IELTS 写作观点。
- 目标词每篇只能出现一次，并应分散到自然完整的句子中，禁止用逗号连续罗列；提高语法价值不能以堆砌长句或拉长篇幅为代价。
- 中文翻译必须与当前英文正文属于同一篇文章，禁止在生成失败或字段缺失时套用演示译文、历史译文或其他学习日的译文。
- 每个目标词必须返回 `translationZh`，且该连续中文文本必须能在当前全文翻译中逐字定位；只有“英文目标词完整覆盖 + 中文全文存在 + 全部逐词中文定位成功”同时通过时才能保存为 AI 短文。
- 缺少合格翻译时必须明确显示暂无对应翻译，不得用英文释义冒充中文，也不得显示无法对应的虚假划线。
